// JavaScript para criar a interface e a lógica do aplicativo
const form = document.getElementById('debt-form');
const debtList = document.getElementById('debt-list');

// Função para adicionar uma nova dívida (armazenando no localStorage)
function addDebt() {
  // ... lógica para coletar os dados do formulário e armazenar no localStorage
}

// Função para exibir a lista de dívidas (recuperando do localStorage)
function displayDebts() {
  // ... lógica para recuperar os dados do localStorage e criar elementos HTML
}

// ... outras funções para editar, excluir e calcular dívidas

// Eventos para os botões e formulários
form.addEventListener('submit', addDebt);
